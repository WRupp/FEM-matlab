function calcReacoes(U,Fglobal)
    % Calcula as reacoes, repondo as linhas/colunas eliminadas
    
    fim = size(salvaEq,1)
    
    for i = 1 :size(salvaEq,1)
    nG = salvaEq(i,1);
    Fglobal(nG) = salvaEq(i,2:end) * U;
    Kglobal(nG,:)=
    end

end